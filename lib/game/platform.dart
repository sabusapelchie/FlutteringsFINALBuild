import 'package:flutter/material.dart';

class Platform {
  double x;
  double y;
  double width;
  double height;
  Color color;
  Color glowColor;

  Platform({
    required this.x,
    required this.y,
    required this.width,
    required this.height,
    required this.color,
    required this.glowColor,
  });
}
